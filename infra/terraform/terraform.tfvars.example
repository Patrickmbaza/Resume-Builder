aws_region               = "us-east-1"
ecr_repository_name      = "resume-builder"
create_apprunner_service = false

# Set to true after your first image is pushed to ECR.
# create_apprunner_service = true

# image_tag = "latest"
# apprunner_service_name = "resume-builder-prod"

tags = {
  Project     = "resume-builder"
  Environment = "production"
  ManagedBy   = "terraform"
  Owner       = "platform-team"
}
